{{ $config := (datasource "config") }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-redis
  namespace: {{ $config.kube-redis.namespace }}
data:
  redis-config: |
    maxmemory 2mb
    maxmemory-policy allkeys-lru
